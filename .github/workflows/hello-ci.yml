name: Hello CI

on:
  push:
    branches:
      - feature/github-actions

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: say Hello
        run: echo "Hello from CI"
