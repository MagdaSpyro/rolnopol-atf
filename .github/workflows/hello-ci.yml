name: Hello CI

on:
  push:
    branches:
      - copilot/worktree-2026-04-22T16-29-12
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-22.04
    steps:
      - name: Print hello message
        run: echo "Hello from CI"
